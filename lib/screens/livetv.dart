import 'package:flutter/material.dart';

class LiveTv extends StatelessWidget {
  const LiveTv({super.key});

  @override
  Widget build(BuildContext context) {
   return const Scaffold(
      body: Center(child: Text("LIve Tv"),),
    );
  }
}